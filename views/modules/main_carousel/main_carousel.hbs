<div class="swiper-container">
    <div class="swiper-wrapper">
        {{#each banners}}
            <div class="swiper-slide"><img src="{{this.url}}" alt="" /></div>
        {{/each}}
    </div>
    <!-- Add Pagination -->
    <div class="swiper-pagination"></div>
</div>
